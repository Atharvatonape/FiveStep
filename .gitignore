# Terraform
terraform/.terraform/
terraform/bootstrap/.terraform/
terraform/bootstrap/terraform.tfstate
terraform/bootstrap/terraform.tfstate.backup
*.tfstate
*.tfstate.*
*.tfvars
tfplan

# Local secrets / config (each user creates their own)
.env

# Personal interview prep notes (keep local)
INTERVIEW.md

# OS
.DS_Store
