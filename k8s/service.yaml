apiVersion: v1
kind: Service
metadata:
  name: fivestep
  labels:
    app: fivestep
spec:
  type: ClusterIP
  selector:
    app: fivestep
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
